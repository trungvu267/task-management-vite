import { useNavigate } from "react-router";
import { BoardHeader, MainLayout } from ".";
export const ReportLayout = () => {
  return (
    <MainLayout>
      <BoardHeader />
    </MainLayout>
  );
};
